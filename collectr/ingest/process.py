def link_cleanup(link):
    """Remove extra patterns from an URL (such as utm_source)

    Args:
        link: str, the link to process
    """
    pass


def index_link(link, sharer=None, media=None, metadata=None):
    """Main entry point when a link is submitted

    Args:
        link: str, the link to process
        sharer: dict, the person who shared this link
        media: dict, the mean the link is shared from
        metadata: dict, the additionnal informations

    """
    pass
